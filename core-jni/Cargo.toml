[package]
name = "silent-caption-jni"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
silent-caption-core = { path = "../core" }

[lints.clippy]
all = "deny"
pedantic = "deny"
