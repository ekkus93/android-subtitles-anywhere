{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; case \"$f\" in */core-jni/*.rs) cargo fmt --manifest-path core-jni/Cargo.toml -- \"$f\" ;; */core/*.rs) cargo fmt --manifest-path core/Cargo.toml -- \"$f\" ;; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
